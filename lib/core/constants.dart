/// See docs/PRODUCT.md and docs/DECISIONS.md for the reasoning behind these
/// numbers before changing them.
class AppConstants {
  AppConstants._();

  static const int freeScanLimit = 400;
  static const String unlockProductId = 'itscans_unlock_lifetime';
}
